These two OFL texts are deliberate duplicates of `src/renderer/fonts/LICENSE-*.txt`: the copy next to the fonts satisfies the OFL requirement that the license accompany the font files (it is sealed inside `app.asar`), while this copy is routed through `extraResources` into `Contents/Resources/licenses/` so it is actually readable from Finder via the app's Third-Party Licenses menu item.
